/* Some custom build time substitutions, found in the global config, are
 * available to this file.  Use them if desired.
 * {IRC_ID} - Two character IRC ID
 * {IRC_DOMAIN} - IRC domain (ie: "example.org")
 * {IPADDR} - The public facing IP Address of the server
 */

/* If using the script, this block will be
 * autogenerated with a random server ID that
 * begins with "1".  
 */
me {
	name "10.155.0.2";
	info "MPTCP Aircraft IRC Server";
	sid "1{IRC_ID}";
};

/* Supply Server Admin Information */
admin {
	"Server Admin Full Name";
	"irc-admin-login-id";
	"server-admin-email@example.org";
};
oper irc-admin-login-id {
	class opers;
	mask *@*;
	password "my-admin-password-here";
	operclass netadmin;
	swhois "is a Network Administrator";
};

/* Port configuration should not need adjustment!
 */
listen {
	ip *;
	port 6667;
};
listen {
	ip *;
	port 6668;
	options { ssl; };
};
listen {
	ip *;
	port 4401;
	options { ssl; serversonly; };
	#options { serversonly; };
};

/* The link/connection over the PPP interface.
 * This should not need modification from a default
 * installation.  Password needs to be coordinated
 * with the aircraft IRC server.
 */
link 10.155.0.1 {
  outgoing {
    bind-ip *;
    hostname 10.155.0.1;
		port 4401;
		options { autoconnect; ssl; };
  };
  password "PPP-NDHCO34N6V";
  hub *;
  class servers;
};

/* If there are any incoming servers, add the links
 * here.
 */
/*
link some-other-aircraft-irc-server {
  incoming {
    mask *;
    options { ssl; };
  };
  password "some-other-aircraft-irc-server-password";
  hub *;
  class servers;
};
*/

/* IRCOp's who have restart/die rights can restart
 * the IRC Server through /RESTART and kill the
 * server with the /DIE command. This block allows
 * you to set a password for these commands so
 * IRCOp's need to use /DIE password and /RESTART
 * password. */

drpass {
	restart "default-restart-password";
	die "default-die-password";
};

/* If you want to allow clients to connect to this
 * server you will need to add some allow blocks.
 */
/*
allow {
  ip *@*;
  class clients;
  maxperip 20;
};
*/
